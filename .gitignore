venv
venv_linux
.venv
.python-version
__pycache__
.ruff_cache
.pytest_cache
.env
# Credential-bearing variants (.env-local, .env-remove, .env.production, ...).
# .env.example is the tracked template and is kept out of the pattern.
.env-*
.env.*
!.env.example
.claude/settings.local.json
.claude/RESUME.md
.claude/tdd-guard/
coverage.xml
.coverage

# Local all-in-one stacks (Gramps Web + MCP on one host) - not shipped
docker-compose-pgsql.yml
docker-compose-sqllite.yml
docker-compose-homelab.yml
docker/

# Local editor config and generated research output
.vscode/
output/

# MkDocs build output
site/

# Local backups of the live tree: Gramps XML plus the media archive.
# Real genealogy data on living people - this repository is public.
backup/
